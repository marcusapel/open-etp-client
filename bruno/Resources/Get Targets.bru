meta {
  name: Get Targets
  type: http
  seq: 6
}

get {
  url: {{host}}/dataspaces/{{dataspaceId}}/resources/{{dataObjectType}}/{{guid}}/targets
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
