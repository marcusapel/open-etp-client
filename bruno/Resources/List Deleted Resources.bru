meta {
  name: List Deleted Resources
  type: http
  seq: 11
}

get {
  url: {{host}}/dataspaces/{{dataspaceId}}/deleted
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
