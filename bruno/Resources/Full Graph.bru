meta {
  name: Full Graph
  type: http
  seq: 10
}

get {
  url: {{host}}/dataspaces/{{dataspaceId}}/graph/all
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200 or 404", function () {
    expect(res.getStatus()).to.be.oneOf([200, 404]);
  });
}
