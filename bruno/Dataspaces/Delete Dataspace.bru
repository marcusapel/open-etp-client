meta {
  name: Delete Dataspace
  type: http
  seq: 7
}

delete {
  url: {{host}}/dataspaces/{{testDataspaceId}}
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200 or 204 or 404 or 412", function () {
    expect(res.getStatus()).to.be.oneOf([200, 204, 400, 404, 412]);
  });
}
