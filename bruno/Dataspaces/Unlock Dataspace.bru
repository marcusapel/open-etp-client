meta {
  name: Unlock Dataspace
  type: http
  seq: 6
}

delete {
  url: {{host}}/dataspaces/{{dataspaceId}}/lock
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200 or 404", function () {
    expect(res.getStatus()).to.be.oneOf([200, 204, 404]);
  });
}
