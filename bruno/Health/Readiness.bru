meta {
  name: Readiness
  type: http
  seq: 2
}

get {
  url: {{host}}/health/readiness
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
