meta {
  name: List Objects
  type: http
  seq: 2
}

get {
  url: {{host}}/witsml/{{dataspaceId}}/objects?type=Well
  body: none
  auth: none
}

headers {
  data-partition-id: {{data-partition-id}}
  Authorization: Bearer {{access_token}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
