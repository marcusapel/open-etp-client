rdms_rest_lint:
  image: node:18.18
  tags: ["osdu-small"]
  stage: scan
  needs: ["compile-and-unit-test"]
  script:
    - npm install
    - npm run lint
  only:
    changes:
      - devops/**/*
      - src/**/*
    refs:
      - branches
      - main
      - merge_requests
