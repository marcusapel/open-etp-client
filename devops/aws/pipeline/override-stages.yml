# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include:
  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/aws-global-variables.yml"
  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/aws-global.yml"

aws-containerize:
  extends:
    - .aws
    - .aws_common_variables
    - .aws_variables
  needs: ["compile-and-unit-test"]
  stage: containerize
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - $(aws ecr get-login --no-include-email --region $AWS_ECR_REGION)
    - docker buildx create --driver docker-container --use
    - docker buildx inspect --bootstrap
    - docker buildx build --platform linux/arm64/v8,linux/amd64 --pull --push -f providers/aws/build-aws/Dockerfile.runtime --build-arg DIST=dist.tgz -t $AWS_IMAGE_TAG_BASE:$CI_COMMIT_SHA -t $AWS_IMAGE_TAG_BASE:latest .
  only:
    variables:
      - $AWS == '1'

aws_test:
  image:
    name: node:lts
  tags: ["osdu-medium"]
  allow_failure: true
  extends:
    - .aws_common_variables
    - .aws_variables
  stage: integration
  needs: ["aws-update-tf"]
  before_script:
    - apt-get update && apt-get install -y jq unzip
    - |
      curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
      unzip -q awscliv2.zip
      ./aws/install
    - !reference [.aws_variables, before_script]
    - |
      echo "AWS_COGNITO_USER_POOL_ID: $AWS_COGNITO_USER_POOL_ID";
      export AWS_TOKEN_FETCH_URL=$(aws ssm get-parameter --name /osdu/idp/${IDP_NAME}/oauth/token-uri --query Parameter.Value  --output text --region $AWS_REGION);
      export AWS_COGNITO_REFRESH_TOKEN=$(aws cognito-idp initiate-auth --auth-flow ${AWS_COGNITO_AUTH_FLOW} --client-id ${AWS_COGNITO_CLIENT_ID} --auth-parameters USERNAME=${AWS_COGNITO_AUTH_PARAMS_USER},PASSWORD=${AWS_COGNITO_AUTH_PARAMS_PASSWORD} | jq -r '.[].RefreshToken // empty');
      echo $AWS_COGNITO_REFRESH_TOKEN | wc -c
    - >
      ACCESS_TOKEN=$(curl -s -X POST "${AWS_TOKEN_FETCH_URL}"
      -d "grant_type=refresh_token"
      -d "client_id=${AWS_COGNITO_CLIENT_ID}"
      -d "refresh_token=${AWS_COGNITO_REFRESH_TOKEN}"
      | node -e "process.stdin.resume();let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>process.stdout.write(JSON.parse(d).access_token))")
  script:
    - npm install -g @usebruno/cli
    - export BRU_ENV="--env CI --env-var access_token=${ACCESS_TOKEN} --env-var RESERVOIR_DDMS_HOST=${API_ENDPOINT}/api/reservoir-ddms/v2 --env-var DATA_PARTITION_ID=${DATA_PARTITION_ID} --env-var ENTITLEMENTS_DOMAIN=${ENTITLEMENTS_DOMAIN}"
    - cd bruno
    - bru run _Setup  $BRU_ENV
    - bru run Health   $BRU_ENV
    - bru run Dataspaces $BRU_ENV
    - bru run Resources $BRU_ENV
    - bru run Arrays    $BRU_ENV
    - bru run Transactions $BRU_ENV
    - bru run Query     $BRU_ENV
    - bru run _Cleanup  $BRU_ENV || true

  only:
    variables:
      - $AWS == '1'
